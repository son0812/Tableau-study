##CH1. 태블로 이해하기

### 1. 디지털로의 전환
- DT : 디지털트랜스포메이션(기업 당면 과제)
  
→ 산재되어 있는 데이터를 집중화 시키고 비즈니스에 적극적으로 활용할 수 있도록 하고 데이터에 대한 내부 문화가 바뀌두록 유도
- DL : 데이터 리터리시 (기업 구성원의 당면 과제)
  
→ 데이터를 보고 활용할 수 있는 능력
- DV : 데이터 비주얼리제이션 (데이터 리터리시를 강화하는 방법)
  
→ 데이터를 시각적으로 표현하고 찾은 인사이트를 조직 내 공유 및 협업하는 과정

- 탐색을 통해 이해하고 이를 다른 사람과 커뮤니케이션, 협업을 통해 인사이트를 도출하고 공유함
- 태블로를 도입하기 전에는 SAP 데이터 다운 후 엑셀에서 가공하고 시각화하였는데, 이제는 SAP 데이터를 태블로에서 바로 연결하고 실시간으로 데이터의 흐름 추적 가능
- Tableau Desktop 에서 시각적 분석 후 Tableau Server로 공유
- 데이터 시각화 : 데이터에 색상과 모양 그리고 여러 요소를 배치해서 데이터를 시각적으로 표현한 것
- 데이터 시각적 분석 : 데이터 시각화를 통해 스토리텔링 및 인사이트를 찾는 분석 기법
----
### 2. 태블로 이해 및 설치하기
- 태블로란? : 데이터를 분석 및 시각화하는 BI(Business Intelligence) 솔루션
- BI : 비즈니스를 운영하면서 얻은 데이터를 수집, 저장, 분석하여 성과를 최적화하는 프로세스와 방법을 망라하는 포괄적 용어 (데이터를 통해 의사결정을 돕고 비즈니스 목표를 달성하게 유도)
- 태블로는 드래그 앤 드롭이나 더블클릭만으로도 시각화 가능 + 데이터 원본에 없는 필드 생성 가능 (엑셀과 유사한 함수) + 모바일 대시보드도 가능
- csv 형태의 파일은 `텍스트파일`로 연결
- 태블로 아이콘을 누르면 작업창이 뜨고 `데이터에 연결`을 선택 혹은 db모양의 아이콘을 선택하면 데이터 선택 가능
- 데이터 그리드에서는 원본데이터를 미리보기 가능
<img width=50% alt="image" src="https://github.com/user-attachments/assets/80282037-034e-40e5-8d2f-27bb359cb7e5" />

- `테이블 세부 정보`를 선택하면 아래와 같이 데이터 원본 필드의 구조를 빠르게 파악 가능
<img width=50% alt="image" src="https://github.com/user-attachments/assets/cf7a93c7-e0cb-4191-b006-276f9ef59bac" />

- 그리고 `데이터 원본`이 아니라 `시트`를 선택해야 해당 시트의 테이블을 선택해서 시각화가 가능
<img width=40% alt="image" src="https://github.com/user-attachments/assets/b3be7eac-3ae2-46ca-97a4-1c6b19e9b9ab" />

- 태블로 퍼블릭은 무료 라이센스이기 때문에 제약 有

----
###  3. 태블로 기본 컨셉 이해하기 (1)
- 📍측정값 : `숫자 형식` , `액션(drag-drop 또는 double-click)`을 통해 설정된 `집계`에 따라 `차트`를 생성
- 📍차원 : 해당 숫자들로 만들어진 차트를 `어떻게 나눠서 볼 것인지`를 결정

<img width=50% alt="image" src="https://github.com/user-attachments/assets/5d86e6b0-7033-418b-befe-540272755848" />

→ 이와 같은 방식으로 해당 측정값이 어떻게 구성되어있는지 확인 가능

<img width=50% alt="image" src="https://github.com/user-attachments/assets/27063796-1af7-440d-bb0b-2286cb3a0367" />

→ 시각화 중에도 다음과 같이 측정값의 집계방식을 변경할 수 있음 (합계를 평균으로)

→ 그렇지만 이렇게 변경한 것은 해당 시트에만 해당 되는 것이지 새로 워크시트를 만든다고 해서 변경된 측정값이 그대로 반영되는 것은 아니다 

  (시트2에서 바꿨던 것처럼 `평균`으로 나오는 것이 아니라 기존의 측정값인 `합계`로 나타난다)

- 측정값의 집계 속성을 완전히 바꾸고 싶다면 아래와 같이 사이드 바에서 `기본속성` > `집계` > `평균`으로 선택해서 바꿔주면 된다
<img width=50%  alt="image" src="https://github.com/user-attachments/assets/fa042324-ba53-49ff-8a4c-f4b42c903e43" />

- `해당 측정값` > `기본속성` > `숫자형식`으로 숫자형식을 백분율, 통화, 사용자 지정 등으로 변경 가능

- 📍차원(Dimension) = 데이터를 `나누는 기준` 즉 **그룹핑 기준**
- 📍측정값(Measure) = 데이터를 `계산하는 값` 즉 **자동 집계되는 값**
<img width=50% alt="image" src="https://github.com/user-attachments/assets/3e262ab4-a4d0-4e80-9358-3e51a24bbc27" />

→ 나는 이를 python 시각화 라이브러리인 seaborn으로 치자면 차원은 `hue`이자 `x축`이자 `그룹 기준`이 되고, 측정값은 `y값` (집계되는 값)으로 이해했다.

(1) 막대차트
- 만들기 쉽고, 항목별로 나눠서 보는데 적합
- 카테고리(범주), 순위, 추세를 보는데 유용함
- 태블로에서는 측정값에서 초록색 연속형 필드를 더블 클릭하면 기본적으로 막대차트가 생성 (위경도 제외)
→ 태블로에서는 측정값(연속형)을 더블 클릭하면 기본적으로 집계된 차트가 생성되며, 이후 차원을 추가하면 해당 차원을 기준으로 데이터가 분할되어 시각화가 재구성된다.

- 행과열을 바꾸는 법
<img width=50% alt="image" src="https://github.com/user-attachments/assets/ed7f49b4-c8d6-49da-866b-bceb83628510" />

+ 좌측에 뜨는 `마크` 사이드 바에서 >`레이블`>`마크레이블 표시`를 선택하면 레이블을 커스텀 할 수 있음
+ 좌측 `패널`사이드 바에서 `기본값`>`숫자`를 통해서 레이블 내부의 단위 변경 가능
<img width=50% alt="image" src="https://github.com/user-attachments/assets/44703704-d555-49f1-95fb-da9210d2fe94" />

- 좌측 `데이터` 사이드바에서 오른쪽 버튼을 크릭해서 `계산된 필드 만들기`를 선택하거나 아래와 같은 방법으로도 `계산된 필드 만들기`가 가능하다
→ 이를 `색상`에 적용하면 이를 기준으로 색상이 나뉨

<img width=50% alt="image" src="https://github.com/user-attachments/assets/298b27ff-3945-493c-9ed8-bda2d70e1595" />

- 좌측 `분석`탭에 들어가서 `평균라인`을 선택하고 시트쪽으로 drag-drop하면 `참조선 추가`가 뜨는데 이를 통해 참조선을 생성할 수 있음
<img width=50% alt="image" src="https://github.com/user-attachments/assets/6f6481d1-0a4a-4732-ad09-e5506a5ab01f" />

----
### 4. 태블로 기본 컨셉 이해하기 (2)
- ⭐태블로에서 `파란색`으로 되어있는 필드는 `불연속형` & `초록색`으로 되어있는 필드는 `연속형`이다.
- `마크`>`색상`에서 불연속형 값을 넣었을 때의 범례는 1번 사진처럼 색상이 구분되고, 연속형 값을 넣었을 때는 2번 사진처럼 색상이 그라데이션 형태로 나타난다.
- 범례의 색상을 변경하고 싶으면 범례를 오른쪽 마우스로 클릭하여 `색상 편집`을 선택하면 불연속형이든 연속형이든 색상 변경 가능
<img width="214" height="93" alt="image" src="https://github.com/user-attachments/assets/97ac097d-4fd7-4ca7-b292-a172d51bf865" />
 
